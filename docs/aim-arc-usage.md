# 枪械瞄准提示

备份：`D:/Desktop/测试/backups/aim-arc-20260917-123439`。

复用玩家预制体的 `attack_ranged` 节点，移除黑色填充扇形，仅绘制射程末端的细弧和空心十字准心。弧线仍使用原有射程和蓄力散布角（默认 60° 收拢到 20°）；中心准心表示方向，不代表保证命中的位置。仅拖动远程攻击时显示，释放、取消和禁用后隐藏。

不修改攻击释放参数、命中判定或子弹散布。手电照明保持原样。瞄准颜色可在攻击组件的“瞄准提示颜色”调整，原填充颜色属性保留兼容旧数据，但不再绘制填充。

收拢完成后，准心和散布弧一起变为红色（默认 `#ff4545`），由“瞄准收拢完成颜色”控制。切换时点使用现有 `rangedChargeVisualDuration`，重新开始瞄准恢复普通颜色。已检查完成前、完成时及重新瞄准的颜色切换，编译通过。本次备份：`D:/Desktop/测试/backups/aim-ready-red-20260917-164431`。

复用弧线坐标数组，满蓄力且方向、射程和样式不变时停止重复绘制。无需新增 Sprite、纹理或实时滤镜。

验证：TypeScript 检查；弧线半径与散布端点、左右翻转、静止 120 次不重画、隐藏再显示、原释放与蓄力计算未改动。通过 MCP 实际拖动和释放，核对瞄准提示与手电叠加效果。
