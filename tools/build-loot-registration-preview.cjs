const fs=require('fs');
const report=JSON.parse(fs.readFileSync('docs/loot-frame-registration.json','utf8').replace(/^\uFEFF/,''));
if(report.length!==54||report.some(r=>r.transform.after>r.transform.before+.001))throw Error('Registration regression');
const entries=[1,2,3].flatMap(batch=>JSON.parse(fs.readFileSync(`assets/config/loot-points-v${batch}.json`,'utf8')).items.map(r=>({...r,batch})));
const cards=entries.map(r=>`<article><h2>${r.name}</h2><div class="pair">${['before','after'].map(mode=>`<div><p>${mode==='before'?'修正前':'修正后'}</p><img data-root="${mode==='before'?'../art/loot-frame-stabilization-before':'../assets/animation/container'}/loot-points-v${r.batch}/${r.id}/" src="${mode==='before'?'../art/loot-frame-stabilization-before':'../assets/animation/container'}/loot-points-v${r.batch}/${r.id}/frame_00.png"></div>`).join('')}</div></article>`).join('');
fs.writeFileSync('docs/loot-frame-stabilization.html',`<!doctype html><meta charset="utf-8"><title>物资点动画对齐对比</title><style>body{background:#26302f;color:#eee;font:16px sans-serif}main{display:grid;grid-template-columns:repeat(2,1fr);gap:20px}article{background:#3a4643;padding:12px}.pair{display:flex}.pair>div{width:50%}img{width:100%;background:repeating-conic-gradient(#3d4a47 0 25%,#43504d 0 50%) 0/24px 24px}h2{font-size:18px}</style><h1>固定柜体对齐 · 修正前后</h1><p>同步循环播放四帧。位置／统一缩放已校正；生成原稿的局部纹理和轮廓差异仍可能存在。</p><button onclick="paused=!paused">暂停／继续</button><main>${cards}</main><script>let frame=0,paused=false;const sequence=[0,0,1,2,3,3,3,0];setInterval(()=>{if(paused)return;const n=sequence[frame++%sequence.length];document.querySelectorAll('img').forEach(img=>img.src=img.dataset.root+'frame_0'+n+'.png');},180);</script>`);
console.log('PASS: 54 registrations do not increase stationary-body comparison error; generated 18 comparison previews');
