const fs=require('fs'),path=require('path');
const metas=new Map();function scan(dir){for(const e of fs.readdirSync(dir,{withFileTypes:true})){const p=path.join(dir,e.name);if(e.isDirectory())scan(p);else if(p.endsWith('.meta')){try{metas.set(JSON.parse(fs.readFileSync(p)).uuid,p.slice(0,-5));}catch{}}}}scan('assets');
const files=[];function collect(dir){for(const e of fs.readdirSync(dir,{withFileTypes:true})){const p=path.join(dir,e.name);if(e.isDirectory())collect(p);else if(p.endsWith('.lh'))files.push(p);}}collect('assets/prefab/prefab_interface');
for(const f of files){if(process.argv[2]&&!f.toLowerCase().includes(process.argv[2].toLowerCase()))continue;console.log('\nFILE '+f);function walk(n,depth=0){const image=n.src||n.texture?._$uuid;console.log(' '.repeat(depth)+(n._$id||'@'+JSON.stringify(n._$override))+' '+(n.name||'')+' '+(n._$type||'prefab')+' '+[n.x||0,n.y||0,n.width||0,n.height||0].join(',')+(n.text?' TEXT='+n.text:'')+(image?' IMAGE='+ (metas.get(image.replace('res://',''))||image):'')+(n._$prefab?' PREFAB='+metas.get(n._$prefab):'')+(n.background?' BG='+JSON.stringify(n.background):''));(n._$child||[]).forEach(c=>walk(c,depth+1));}walk(JSON.parse(fs.readFileSync(f)));}
