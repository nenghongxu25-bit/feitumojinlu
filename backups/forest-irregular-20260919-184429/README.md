# Irregular terrain trial

forest.png and metadata: backup before this trial (rounded, muted-color version).
generated-draft.png: built-in imagegen concept draft; not installed.
forest-irregular.png: final source-texture-preserving boundary displacement result.

Applied to assets/tileset/forest/forest.png without changing TileSet metadata or scenes.
8-pixel guard on every 128-pixel tile, six center tiles and alpha are unchanged.
Use tools/irregular-forest-terrain.ps1 with this backup as input to reproduce; do not repeatedly apply to the already processed output.
Restore forest.png to assets/tileset/forest/forest.png to undo this trial.
